Custom filers I use for uBlock Origin. <br/>
To add this to your config, open uBO settings > My filters > Import and append (then select the `filters.txt` file). <br/>
Alternatively, just copy paste the contents of `filters.txt` into the "My filters" section.
